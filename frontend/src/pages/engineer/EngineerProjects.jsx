import ProjectsPage from '../shared/ProjectsPage';

const EngineerProjects = () => (
  <ProjectsPage title="My Projects" subtitle="Paid work, progress updates, and delivery" />
);

export default EngineerProjects;
