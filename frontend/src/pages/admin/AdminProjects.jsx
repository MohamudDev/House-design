import ProjectsPage from '../shared/ProjectsPage';

const AdminProjects = () => (
  <ProjectsPage title="All Projects" subtitle="Monitor every client–engineer project" />
);

export default AdminProjects;
