import ProjectsPage from '../shared/ProjectsPage';

const AdminProjects = () => (
  <ProjectsPage title="All Projects" subtitle="Monitor every client–engineer delivery project" />
);

export default AdminProjects;
